<p id="notice"><%= notice %></p>

<h1>Prices</h1>

<table>
  <thead>
    <tr>
      <th>Vendor</th>
      <th>Product</th>
      <th>Price</th>
      <th>Quantity</th>
      <th>Quantity discount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @prices.each do |price| %>
      <tr>
        <td><%= price.vendor %></td>
        <td><%= price.product %></td>
        <td><%= price.price %></td>
        <td><%= price.quantity %></td>
        <td><%= price.quantity_discount %></td>
        <td><%= link_to 'Show', price %></td>
        <td><%= link_to 'Edit', edit_price_path(price) %></td>
        <td><%= link_to 'Destroy', price, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Price', new_price_path %>
